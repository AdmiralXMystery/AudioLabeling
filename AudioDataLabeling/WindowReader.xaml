<Window x:Class="AudioDataLabeling.WindowReader"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AudioDataLabeling"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="WindowReader" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="JSON Content" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0 15 0 10"/>
        <StackPanel Grid.Row="1" Margin="15,0,15,10">
            <TextBox Padding="4" FontFamily="Segoe UI" VerticalAlignment="Center"
                         Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding MatchCountText}" 
                       FontSize="11" 
                       Foreground="Gray"  
                       HorizontalAlignment="Center" 
                       FontFamily="Segoe UI"/>
        </StackPanel>
        <FlowDocumentScrollViewer Grid.Row="3" Margin="15,0,15,15" FontFamily="Segoe UI"
                                  Document="{Binding Document}"
                                  VerticalScrollBarVisibility="Visible" BorderBrush="Gray" BorderThickness="1"/>
    </Grid>
</Window>
